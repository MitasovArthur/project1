package Prectik_2;

public class MathPow {
    public static void main(String [] args){
        //Класс матх всегда доступен и содержит много рахных методов


        double x = 2;
        double y = 8;
        double result = Math.pow (x, y);//в данном случае мы возводим в квадрат х число а y степень
        //*double result = Math.pow (x, y); // если мы хочем продолжить работать в другом формате

        System.out.println(result);
    }
}
